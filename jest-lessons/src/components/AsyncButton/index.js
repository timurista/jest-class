import React from 'react';
import styled from 'styled-components';

const logger = () => null;


export default class AsyncButton {
  onClick = () => {
    // do some logging
    logger = 
    this.props.onClick()
  }
  
  return (
    <Button  />
  )
}
